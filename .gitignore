node_modules
dist
.next
*.tsbuildinfo

# Native host (Go binaries)
apps/native/dist/

# Extension (managed separately)
# apps/extension/
apps/mvp-chromium-extension/
old-native/

# Local environment variables
.env
.env.local

# Extension signing key (keep secret!)
extension-key.pem

# Auto-generated extension source (build-dev.ts) — except the committed placeholder
apps/extension/src/generated/*
!apps/extension/src/generated/bundled-presences.ts